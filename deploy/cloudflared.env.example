# 复制为 ~/.config/ufi-web/cloudflared.env，仅保存在设备，不要提交到 Git。
export CLOUDFLARED_BIN="$PREFIX/bin/cloudflared"
export CLOUDFLARED_CONFIG="$HOME/.cloudflared/config.yml"
export CLOUDFLARED_TUNNEL=""
# 若使用 token 模式，将 token 只写在设备本地此文件；不要提交到 Git。
export CLOUDFLARED_TOKEN=""
